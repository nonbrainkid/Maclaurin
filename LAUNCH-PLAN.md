# $MACLRN — план до лаунча

Статус на 2026-08-01: фазы 1 и 2 реализованы в одном байткоде, **124 теста зелёные**
(1 пропускается без fork-RPC), Slither — 0 находок, `forge fmt` чист. Независимый
аудит пройден и по фазе 1, и по механике фазы 2 — Critical/High/Medium ноль в обоих;
единственный Low (фаза 1) исправлен, в фазе 2 нет и Low. Последний шаг (сам лаунч)
выполняет Егор вручную.

Бюджет: **$15**. Сеть: **Robinhood Chain** (chain ID 4663, Arbitrum Orbit L2,
газ в ETH). Решение о смене сети с Base принято 2026-08-01: нарратив требует
цепи, принадлежащей компании Тенева, а код не меняется — сеть полностью
EVM-совместима.

**Где мы сейчас:** этапы 1 и 2 закрыты. Тестнет Robinhood Chain пройден,
контракты развёрнуты и верифицированы, весь цикл проверен в живой сети.
Мейннет заблокирован решением 0.1 — оно остаётся открытым до тестнета.

---

## Этап 0. Блокирующие решения (принять ДО любого деплоя)

Контракты неизменяемы. Всё ниже после деплоя не переигрывается.

### 0.1 Распределение GENESIS — 🔴 ОТКРЫТО. Решение отложено до тестнета (2026-08-01)

**Статус: не принято.** Егор фиксирует доли после прохождения этапа 2, на реальных
цифрах. Деплой в мейннет без этого решения невозможен: `GENESIS_RECIPIENT`
передаётся в конструктор и immutable — переиграть после деплоя нельзя.

**В чём проблема.** Сейчас все **2 000 000 000 токенов (73.576% сапплая)** уходят
на один адрес `GENESIS_RECIPIENT`. Для любого чекера (Token Sniffer, Honeypot.is,
DexScreener) это красный флаг номер один: «73% у одного кошелька» — стандартный
маркер рагпула, и он выставляется автоматически, без разбирательств. Нарратив
«нет mint, нет владельца» здесь не спасает: чекер смотрит на распределение
холдеров, а не на байткод.

**Что надо решить** — конкретные доли (цифры ниже предложенные, не утверждённые):

| Назначение | Предлагаемая доля | Обоснование |
|---|---|---|
| Ликвидность в пул (LP) | 40–60% genesis | То, что реально торгуется |
| Заблокировано на N месяцев | 30–50% | Публичный лок с пруфом |
| Казна / маркетинг | ≤10% | Видимая и объяснённая |

**Действие после решения:** genesis-адрес должен раскидать средства сразу после
деплоя, а не сидеть на всей сумме.

**Заметка на полях от этапа 2:** тестнет как раз и нужен, чтобы принимать это
решение на измеренных цифрах — стоимости пула, реальном сдвиге цены на свопе,
поведении чекеров на верифицированном контракте.

### 0.2 Фаза 2 (Radius of Convergence) — ✅ РЕШЕНО И РЕАЛИЗОВАНО (2026-08-01)

Выбран вариант «реализовать множители лока внутри `MaclaurinEmission` до мейннета».
Причина: контракт неизменяем, а тезис «максимальный множитель равен e и
недостижим» — половина нарратива; обещать в GTM то, чего нет в коде, нельзя.

Дизайн зафиксирован в **`PHASE2-SPEC.md`**, там же — отклонения, принятые при
реализации. Ключевое — §3: наивная реализация (множитель сразу + claim всегда
доступен + штраф за досрочный выход) даёт дыру, через которую буст забирают,
не отсидев лок. Закрыто блокировкой `claim()` до `unlockTime`; `unstake()`
остался открыт всегда.

**Что из этого закрыто:**
- механика реализована в `src/MaclaurinEmission.sol`: радиусы 1..7, взвешенный
  аккумулятор по `totalWeight`, `poke(address)`, сжигание награды при досрочном
  выходе;
- тесты переписаны под лок: 124 зелёных вместо прежних 72, из них 33 в
  `test/MaclaurinRadius.t.sol` и 7 независимых проб на обход лока в
  `test/Phase2Probe.t.sol`;
- **повторный независимый аудит изменённой части пройден:** Critical/High/Medium/Low —
  ноль, только Info с подтверждением арифметики весов;
- ломающее изменение ABI (`stake(uint256)` → `stake(uint256,uint256)`) разнесено
  по документации.

Последствие для плана: этап 2 (тестнет) сдвинулся до готовности фазы 2 — и теперь
разблокирован.

### 0.3 Одинокий стейкер забирает эпоху 2 — 🔴 ОТКРЫТО

Измерено пробой `test_probe_LoneDustStaker_TakesEntireEpoch`: адрес со стейком
**1 wei** получает весь член эпохи 2 — **500 000 000 токенов, 18.4% сапплая**, если
за первую неделю не пришёл никто больше.

Это корректная работа pro-rata, а не баг. Но если запуск не соберёт стейкеров в первую
неделю, пятая часть сапплая уйдёт одному наблюдательному адресу.

Фаза 2 это не отменяет: при единственном стейкере доля равна 100% независимо от
множителя (вес и `totalWeight` — одно и то же число). Единственное, что изменилось, —
забрать награду он сможет не раньше своего `unlockTime`, то есть минимум через неделю.

Варианты: принять как есть (честный first-mover), либо задать `EMISSION_START_DELAY`
достаточным, чтобы к старту эпохи 2 уже была аудитория.

### 0.4 Адреса и тайминг — 🔴 ОТКРЫТО

- `GENESIS_RECIPIENT` — какой кошелёк (желательно не тот, с которого деплой)
- `REMAINDER_VAULT` — immutable навсегда, выбрать осознанно. Это то же место, куда
  впоследствии встанет казна фазы 3, так что после мейннет-деплоя адрес уже не
  переиграть
- `EMISSION_START_DELAY` — сейчас 86400 (сутки) в `.env.example`. Хватает ли на
  создание пула? Скрипт деплоя не даёт опустить значение ниже `MIN_START_DELAY = 300`
  секунд — это запас на дрейф `block.timestamp` между симуляцией и реальным блоком,
  без него конструктор ревертит `StartTimeInPast` уже на broadcast

---

## Этап 1. Финализация контрактов — ✅ ЗАКРЫТ

- [x] Прогнать **Slither** (`slither .`), разобрать каждый флаг, задокументировать
      осознанные исключения — 15 контрактов, 98 детекторов, **0 находок**.
      Конфиг в `slither.config.json`, обоснование каждого исключения — в `README.md`
      (раздел «Slither»). Попутно исправлена семантика: было `filter_paths "lib/"`,
      стало `exclude_dependencies` — первый вариант молча глушил бы настоящий
      reentrancy в `stake()`, потому что тот ссылается на `SafeERC20`
- [x] Независимый аудит субагентом `smart-contract-auditor` — **два прохода**:
      по фазе 1 и отдельно по механике фазы 2 (веса, сжигание награды, `poke`)
- [x] Устранить всё, что выше Info; каждое Info — с письменным обоснованием —
      Critical/High/Medium ноль в обоих проходах. Единственный Low из аудита фазы 1
      исправлен: `MIN_START_DELAY = 300` в `script/Deploy.s.sol`. В аудите фазы 2
      нет и Low: только Info с подтверждением арифметики весов
- [x] `forge test` зелёный + `forge coverage` по `src/` — **124 passed, 0 failed,
      1 skipped** (`test_Run_OnFork` пропускается без `DEPLOY_FORK_RPC_URL`).
      Покрытие `script/Deploy.s.sol` доведено с 0% до 100% строк
- [x] Зафиксировать компилятор точной версией (не `^0.8.24`) для воспроизводимого
      билда — `solc = "0.8.24"` и `evm_version = "shanghai"` в `foundry.toml`,
      `pragma solidity =0.8.24` в исходниках
- [x] CI в `.github/` прогоняет тесты на каждый push — `.github/workflows/test.yml`:
      `forge fmt --check`, `forge build --sizes`, `forge test -vvv` и Slither
      с зафиксированной версией `0.11.5`

**Критерий готовности:** ноль находок уровня Medium и выше — **выполнен.**

### Что проверил аудит фазы 2

Арифметика весов подтверждена независимо:

| Величина | Значение |
|---|---|
| Максимальный вес (весь сапплай под R=7) | 7.388e27 |
| Инкремент аккумулятора на эпохе 26 при максимальном весе | 270 (в фазе 1 было 735) |
| Запас `weight × delta` до потолка uint256 | ×1.61e20 |

Вывод: `ACC_PRECISION = 1e30` менять не нужно. Нижняя граница ужалась втрое
относительно фазы 1 и осталась с запасом в два с лишним порядка до нуля —
хвост ряда не обнуляется. Верхняя граница множителем не сдвигается вообще:
он входит и в числитель, и в знаменатель.

---

## Этап 2. Тестнет Robinhood Chain (46630) — ✅ ПРОЙДЕН 2026-08-01

Развёрнуто и проверено в живой сети. Стоимость деплоя — **0.00002498641 ETH**
(2 498 641 газа × 0.01 gwei), вдвое дешевле оценки: реальная цена газа оказалась
0.01 gwei против расчётных 0.02.

| Контракт | Адрес |
|---|---|
| `TaylorToken` (до переименования) | `0x842f0f321aBf977F94A3a55f6c556619a9FB5EDA` |
| `TaylorEmission` (до переименования) | `0x7D4E9AAA793553Fd8d07E3F43c62a8C015fDf3f1` |

> ⚠️ Этот деплой сделан **до** переименования проекта в Maclaurin. В байткоде по
> этим адресам зашиты старые имена (`name` = `"Taylor Series"`, `symbol` = `"TAYLOR"`),
> и изменить их нельзя — контракты неизменяемы. Адреса оставлены как история этапа;
> тестнет нужно передеплоить заново под `MaclaurinToken` / `MaclaurinEmission`.

Деплоер и genesis-получатель: `0x5A443FfaFe4c2A7d16f242eebcbAbb1F46cA7bAA`.
Блок деплоя 95876798, `startTime` 1785574079.

- [x] Тестовый ETH получен из крана
- [x] Деплой одной транзакцией; `_verify()` прошёл, `all post-deploy invariants hold`
- [x] Верификация **обоих** контрактов через Blockscout. `--constructor-args`
      сработал вопреки пометке «Only for Etherscan» в справке forge — для токена,
      рождённого внутри конструктора, обходные пути не понадобились
- [x] Математика сверена на цепочке: `totalSupply` = floor(e×10²⁷),
      `epochAmount(26)` = 2 wei, `epochAmount(27)` = 0, `totalEmittable` = пул
      минус ровно 14 wei, `multiplier(7)` < `E_FIXED`
- [x] `mint()` и `owner()` ревертят — функций нет в байткоде
- [x] Стейк с R=1: вес равен телу до последнего wei, награда начисляется
- [x] **Лок работает:** `claim()` вернул `StillLocked` (селектор `0x16b82bbe`)
      с аргументом, точно равным `unlockTime`; длительность лока — ровно
      604 800 секунд
- [x] Досрочный выход: тело вернулось **целиком, штраф 0 wei**, награда сгорела,
      `totalClaimed` остался нулём, прирост `unallocated` совпал с событием
      `RewardForfeited` до последней единицы (150 462 962 962 962 962 962 962)
- [x] `sweepToRemainderVault` доставил 410 879 токенов в казну; баланс контракта
      сошёлся с пулом минус выплаченное

### Что в тестнете проверить нельзя — и почему

- **Успешный `claim()`** — требует отсидеть 7 дней, `EPOCH_DURATION` это
  `constant`. Проверяется на локальном форке через `evm_increaseTime`
  (`DEPLOY-RUNBOOK.md` §8.0)
- **`poke()` на истёкшей позиции** — та же причина. Отказ на активном локе
  проверен
- **Пул ликвидности** — в тестнете Robinhood Chain нет ни Uniswap, ни WETH.
  Только репетиция на форке мейннета

### Наблюдение для мейннета

`sweepToRemainderVault` не ревертит `NothingToSweep` при повторном вызове, пока
идёт эмиссия и нет стейкеров: `_accrue()` успевает начислить новую порцию между
блоками. `NothingToSweep` возможен только в пределах одного блока или после
конца эмиссии. Поведение корректное, но контринтуитивное.

- [ ] На измеренных цифрах вернуться к решению 0.1 (распределение GENESIS)

---

## Этап 3. Подготовка к мейннету

- [ ] Отдельный кошелёк для деплоя, приватный ключ **не в `.env`** —
      `cast wallet import` или аппаратный кошелёк
- [ ] Купить ETH и вывести **сразу в сети Robinhood Chain** (не бриджить из
      Ethereum — бридж съест весь бюджет)
- [ ] Проверить смету: деплой ~$0.05, пул ~$0.10, остальное — ликвидность.
      Замер на тестнете: вся связка стоила **0.000025 ETH**
- [ ] Финальный `git tag` коммита, который идёт в мейннет

---

## Этап 4. Деплой в мейннет

- [ ] `forge script script/Deploy.s.sol --rpc-url rh --account <keystore>
      --sender <addr> --broadcast` с реальными адресами
- [ ] Инварианты `_verify()` прошли on-chain
- [ ] Записать адреса контрактов в README

---

## Этап 5. Верификация и метаданные

- [ ] Оба контракта верифицированы через **Blockscout** (не Etherscan — у сети
      нет Etherscan-инстанса). Ключ API не нужен. На тестнете `--constructor-args`
      сработал, несмотря на пометку «Only for Etherscan» в справке forge
- [ ] Логотип токена, описание, ссылки — заявка в token info Blockscout
- [ ] Проверить, как токен выглядит в DexScreener (сеть поддерживается,
      `chainId: robinhood`). Token Sniffer и Honeypot.is **chain 4663 не
      поддерживают** — автоматической проверки на рагпул там не будет

---

## Этап 6. Ликвидность

- [ ] Создать пул Uniswap V3 на Robinhood Chain, задать стартовую цену.
      Адреса сверены on-chain: фабрика `0x1f7d7550…`, NPM `0x73991a25…`,
      роутер `0xCaf681a6…`, WETH `0x0Bd7D308…`
- [ ] Внести ликвидность (~$13 при текущем бюджете)
- [ ] **LP-позицию заблокировать или сжечь**, пруф опубликовать
- [ ] Проверить своп в обе стороны реальными деньгами

> Честно о масштабе: с $13 в пуле своп на $3 сдвинет цену на десятки процентов.
> Это не рынок, а работающая демонстрация полного цикла. Для первого мейннет-деплоя
> этого достаточно; наращивать ликвидность — отдельная задача с отдельным бюджетом.

---

## Этап 7. Нарратив и материалы

**Хендл X: `@TaylorSeriesRHC`** — занят 2026-08-01, аккаунт на проверке.
`RHC` читается однозначно как Robinhood Chain, а не как отсылка к брокеру.
Менять после анонса нельзя — сверка хендла с эксплорером входит в базовую
проверку доверия.

> ⚠️ **Расходится с переименованием в Maclaurin.** Хендл занимали под старое имя,
> и с `name` токена (`"Maclaurin Series"`) он больше не совпадает. Поскольку сверка
> хендла с эксплорером — часть базовой проверки доверия, это нужно закрыть **до**
> анонса: занять `@MaclaurinSeriesRHC` (или иной вариант, совпадающий с `name`
> буква в букву) и решить судьбу старого хендла. Задача не закрыта.

- [ ] Лендинг: объяснение ряда, живой счётчик текущей эпохи, таблица эмиссии
- [ ] Раздел «почему здесь нельзя напечатать токены» со ссылкой на верифицированный код
- [ ] Radius of Convergence: таблица множителей и сроков лока. Тезис «потолок равен `e`
      и недостижим» теперь подкреплён кодом — `E_FIXED` в контракте, проверка на цепочке
- [ ] Инструкция для пользователя: как выбрать радиус, застейкать и забрать. Обязательно
      явно: **`claim()` закрыт до конца лока, `unstake()` открыт всегда, досрочный выход
      возвращает тело целиком и сжигает награду**
- [ ] Тексты для X: тред с математикой + хук
- [ ] Пруфы: ссылки на верифицированные контракты, лок ликвидности, отчёт аудита

**Ограничение (из спеки, §9):** нарратив строится вокруг математики, а не вокруг
реального публичного лица. Не использовать имя, изображение или цитаты реальных людей
так, чтобы подразумевалась их причастность.

---

## Этап 8. Лаунч — выполняет Егор

Всё готово, кнопку нажимает человек.

---

## Что делали субагенты

| Агент | Задача | Этап | Состояние |
|---|---|---|---|
| `smart-contract-auditor` | Независимый аудит `src/` (фаза 1) | 1 | ✅ закрыт |
| `smart-contract-auditor` | Независимый аудит механики фазы 2 | 1 | ✅ закрыт, находок нет |
| `general-purpose` | Slither + CI + воспроизводимый билд | 1 | ✅ закрыт |
| `general-purpose` | Реализация фазы 2 по `PHASE2-SPEC.md` | 1 | ✅ закрыт |
| `general-purpose` | Синхронизация документации с кодом | 1 | ✅ закрыт |
| `general-purpose` | Runbook деплоя и подготовка к тестнету | 2–6 | в работе |

Текущий план работ — в `PHASE3-PLAN.md`.
